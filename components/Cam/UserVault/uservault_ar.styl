@import './uservault.styl';

.vault-container
  .vault-card-header
    .tila-credit-wrap
      border: 1px dotted;
      padding: 10px 20px;
      position: absolute;
      left: 40px;
      border-radius: 6px;
      box-shadow: 0px 4px rgba(0, 0, 0, 0.06)

  .vault-card-delete-icon
    left 15px
  .make-default
    padding-right 10px

.card-number
  direction ltr
  text-align right
@import './uservault.styl';

.vault-container
  .vault-card-header
    .tila-credit-wrap
      border: 1px dotted;
      padding: 10px 20px;
      position: absolute;
      left: 40px;
      border-radius: 6px;
      box-shadow: 0 4px 4px 0 rgba(0, 0, 0, 0.16);
